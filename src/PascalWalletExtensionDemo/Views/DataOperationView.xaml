<UserControl x:Class="PascalWalletExtensionDemo.Views.DataOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:PascalWalletExtensionDemo.ViewModels"
             xmlns:Views="clr-namespace:PascalWalletExtensionDemo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type ViewModels:InfoMessageViewModel}">
                <Views:InfoMessageView />
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" FontSize="16" FontWeight="DemiBold" Margin="0,5,0,0" Text="Create chat message on Pascal coin blockchain" HorizontalAlignment="Center" />

        <TextBlock Grid.Row="1" Text="Select sender account" Margin="5" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="5" ItemsSource="{Binding Accounts}" SelectedValue="{Binding SenderAccount}" />
        <TextBlock Grid.Row="2" Margin="5" Text="Select receiver account" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding ReceiverAccount}" />
        <TextBlock Grid.Row="3" Margin="5" Text="Fee" />
        <DockPanel Grid.Row="3" Grid.Column="1">
            <TextBox Text="{Binding Fee}" Margin="5" Width="50" />
            <Slider Value="{Binding Fee}" Minimum="0" VerticalAlignment="Center"  TickPlacement="BottomRight" TickFrequency="0.0001" IsSnapToTickEnabled="True" Maximum="0.001" />
        </DockPanel>

        <TextBlock Grid.Row="4" Margin="5" Text="Identifier" />
        <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Text="{Binding Identifier}" />
        <TextBlock Grid.Row="5" Margin="5" Text="Message" />
        <TextBox Grid.Row="5" Grid.Column="1" Margin="5" Text="{Binding Message}" />



        <Button Grid.Row="6" Grid.ColumnSpan="2" Content="Send" HorizontalAlignment="Center" Padding="10,0" Command="{Binding SendCommand}" />

        <ContentControl Grid.RowSpan="8" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{Binding InfoMessage}" />
    </Grid>
</UserControl>
